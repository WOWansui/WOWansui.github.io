# Schema

No project-specific AI schema is currently defined.

Do not infer or enforce one from old templates. If a future request needs a schema,
propose it first and get user confirmation before adding it here.

Keep this file empty of feature-specific constraints unless the user explicitly asks
to define a project schema.
